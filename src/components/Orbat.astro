---

---

<div class="flex flex-col items-center justify-center h-full">
    <h1 class="text-4xl font-bold">Orbat</h1>
    <div id="orbat" class="h-full w-full"></div>
</div>

<script>
    import { initRender } from "./Orbat/render";

    const div = document.getElementById("orbat");

    if (!div) {
        throw new Error("Element not found");
    }

    initRender(window, div, []);
</script>
